# Claude Code dev files
.claude/

# Throwaway mockup files
mockup-*.html
mockup/

# Node build dependencies — never commit these
node_modules/
package-lock.json

# Build backup — local only, not needed in repo
dist/app.js.bak

# Discord op log dumps — personal data, never commit
ops.txt
results.txt
results.csv
